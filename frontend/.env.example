# Backend API base URL
# Leave empty or omit during local development if using the Vite proxy (http://localhost:5000)
# When deploying to Vercel, set this to your deployed Render backend URL (e.g. https://your-backend.onrender.com)
VITE_API_URL=http://localhost:5000
